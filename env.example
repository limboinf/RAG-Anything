### 这是 .env 的示例文件


### 服务器配置
HOST=0.0.0.0
PORT=9621
WEBUI_TITLE='My Graph KB'
WEBUI_DESCRIPTION="Simple and Fast Graph Based RAG System"
OLLAMA_EMULATING_MODEL_TAG=latest
# WORKERS=2
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

### Tiktoken 缓存配置（用于离线部署）
### 将其设置为包含 tiktoken 模型缓存的本地目录
### 这可防止 tiktoken 在初始化时从互联网下载模型
### 配置说明见 docs/offline_setup.md
# TIKTOKEN_CACHE_DIR=./tiktoken_cache

### 登录配置
# AUTH_ACCOUNTS='admin:admin123,user1:pass456'
# TOKEN_SECRET=Your-Key-For-LightRAG-API-Server
# TOKEN_EXPIRE_HOURS=48
# GUEST_TOKEN_EXPIRE_HOURS=24
# JWT_ALGORITHM=HS256

### 用于访问 LightRAG 服务端 API 的 API-Key
# LIGHTRAG_API_KEY=your-secure-api-key-here
# WHITELIST_PATHS=/health,/api/*

### 可选 SSL 配置
# SSL=true
# SSL_CERTFILE=/path/to/cert.pem
# SSL_KEYFILE=/path/to/key.pem

### 目录配置（默认为当前工作目录）
### 若通过 Docker 部署不应在此设置（由 Dockerfile 配置）
### 默认值为 ./inputs 和 ./rag_storage
# INPUT_DIR=<absolute_path_for_doc_input_dir>

### RAGAnything 配置（多模态文档处理）
### ---
### 解析器配置
# PARSE_METHOD=auto
# OUTPUT_DIR=./output
# PARSER=mineru
# DISPLAY_CONTENT_STATS=true

### 多模态处理配置
# ENABLE_IMAGE_PROCESSING=true
# ENABLE_TABLE_PROCESSING=true
# ENABLE_EQUATION_PROCESSING=true

### 批处理配置
# MAX_CONCURRENT_FILES=1
# SUPPORTED_FILE_EXTENSIONS=.pdf,.jpg,.jpeg,.png,.bmp,.tiff,.tif,.gif,.webp,.doc,.docx,.ppt,.pptx,.xls,.xlsx,.txt,.md
# RECURSIVE_FOLDER_PROCESSING=true

### 上下文抽取配置
# CONTEXT_WINDOW=1
# CONTEXT_MODE=page
# MAX_CONTEXT_TOKENS=2000
# INCLUDE_HEADERS=true
# INCLUDE_CAPTIONS=true
# CONTEXT_FILTER_CONTENT_TYPES=text
# CONTENT_FORMAT=minerU

### 图检索返回的最大节点数
# MAX_GRAPH_NODES=1000

### 日志级别
# LOG_LEVEL=INFO
# VERBOSE=False
# LOG_MAX_BYTES=10485760
# LOG_BACKUP_COUNT=5
### 日志文件位置（默认为当前工作目录）
# LOG_DIR=/path/to/log/directory

### RAG 查询设置
# HISTORY_TURNS=3
# COSINE_THRESHOLD=0.2
# TOP_K=60
# MAX_TOKEN_TEXT_CHUNK=4000
# MAX_TOKEN_RELATION_DESC=4000
# MAX_TOKEN_ENTITY_DESC=4000

### 实体与关系摘要配置
### 语言：English、Chinese、French、German ...
SUMMARY_LANGUAGE=English
### 触发 LLM 重新摘要的重复实体/边数量（建议至少为 3）
# FORCE_LLM_SUMMARY_ON_MERGE=6
### 合并后实体/关系描述的最大 token 数
# MAX_TOKEN_SUMMARY=500

### 并行处理的文档数量（建议小于 MAX_ASYNC/2）
### 文档切分块大小，建议 500~1500
# CHUNK_SIZE=1200
# CHUNK_OVERLAP_SIZE=100

### LLM 配置
ENABLE_LLM_CACHE=true
ENABLE_LLM_CACHE_FOR_EXTRACT=true
### LLM 的超时时间（秒），None 表示无限超时
TIMEOUT=240
### 某些模型（如 o1-mini）需要将 temperature 设为 1
TEMPERATURE=0
### LLM 并发请求上限
MAX_ASYNC=4
### MAX_TOKENS：发送给 LLM 进行实体关系摘要的最大 token 数（应小于模型上下文长度）
### MAX_TOKENS：由 API Server 为 Ollama 设置的 num_ctx 选项
MAX_TOKENS=32768
### LLM 绑定类型：openai、ollama、lollms、azure_openai、lmstudio
LLM_BINDING=openai
LLM_MODEL=gpt-4o
LLM_BINDING_HOST=https://api.openai.com/v1
LLM_BINDING_API_KEY=your_api_key
### Azure 可选项
# AZURE_OPENAI_API_VERSION=2024-08-01-preview
# AZURE_OPENAI_DEPLOYMENT=gpt-4o

### 向量化配置
### 向量化绑定类型：openai、ollama、lollms、azure_openai、lmstudio
EMBEDDING_BINDING=ollama
EMBEDDING_MODEL=bge-m3:latest
EMBEDDING_DIM=1024
EMBEDDING_BINDING_API_KEY=your_api_key
# 如果嵌入服务部署在同一 Docker 栈中，请使用 host.docker.internal 替代 localhost
EMBEDDING_BINDING_HOST=http://localhost:11434
### 单次请求发送给向量化服务的分块数量
# EMBEDDING_BATCH_NUM=32
### 向量化服务的最大并发请求数
# EMBEDDING_FUNC_MAX_ASYNC=16
### 每个分块发送给向量化服务的最大 token 数（可能已废弃？）
# MAX_EMBED_TOKENS=8192
### Azure 可选项
# AZURE_EMBEDDING_DEPLOYMENT=text-embedding-3-large
# AZURE_EMBEDDING_API_VERSION=2023-05-15

### 数据存储选择
# LIGHTRAG_KV_STORAGE=PGKVStorage
# LIGHTRAG_VECTOR_STORAGE=PGVectorStorage
# LIGHTRAG_DOC_STATUS_STORAGE=PGDocStatusStorage
# LIGHTRAG_GRAPH_STORAGE=Neo4JStorage

### TiDB 配置（已弃用）
# TIDB_HOST=localhost
# TIDB_PORT=4000
# TIDB_USER=your_username
# TIDB_PASSWORD='your_password'
# TIDB_DATABASE=your_database
### 将不同 Lightrag 实例的数据隔离（即将弃用）
# TIDB_WORKSPACE=default

### PostgreSQL 配置
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=your_username
POSTGRES_PASSWORD='your_password'
POSTGRES_DATABASE=your_database
POSTGRES_MAX_CONNECTIONS=12
### 将不同 Lightrag 实例的数据隔离（即将弃用）
# POSTGRES_WORKSPACE=default

### Neo4j 配置
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD='your_password'

### 独立 AGM 配置（不适用于嵌入 PostgreSQL 的 AMG）
# AGE_POSTGRES_DB=
# AGE_POSTGRES_USER=
# AGE_POSTGRES_PASSWORD=
# AGE_POSTGRES_HOST=
# AGE_POSTGRES_PORT=8529

# AGE 图名称（适用于 PostgreSQL 与独立 AGM）
### AGE_GRAPH_NAME 已弃用
# AGE_GRAPH_NAME=lightrag

### MongoDB 配置
MONGO_URI=mongodb://root:root@localhost:27017/
MONGO_DATABASE=LightRAG
### 将不同 Lightrag 实例的数据隔离（即将弃用）
# MONGODB_GRAPH=false

### Milvus 配置
MILVUS_URI=http://localhost:19530
MILVUS_DB_NAME=lightrag
# MILVUS_USER=root
# MILVUS_PASSWORD=your_password
# MILVUS_TOKEN=your_token

### Qdrant 配置
QDRANT_URL=http://localhost:16333
# QDRANT_API_KEY=your-api-key

### Redis 配置
REDIS_URI=redis://localhost:6379
